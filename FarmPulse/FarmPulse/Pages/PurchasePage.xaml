<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:view="clr-namespace:FarmPulse.Views"
             x:Class="FarmPulse.Pages.PurchasePage"
             BackgroundColor="{StaticResource AppColor}">

    <Grid RowDefinitions="Auto,*">
        <view:NavigationView Text="{Binding Purchase}"
                             TextColor="Black"
                             BackgroundColor="#D3BCA2"/>
        <Grid RowDefinitions="*,Auto"
              ColumnDefinitions="*,310,*"
              Grid.Row="1">
            <ScrollView Grid.Column="1">
                <Grid RowDefinitions="130,130,130" 
                      ColumnDefinitions="*,*"
                      RowSpacing="15"
                      ColumnSpacing="15"
                      VerticalOptions="CenterAndExpand">
                    <view:PurchaseItemView ImageSource="contact"
                                           Text="{Binding Insurance}"/>
                    <view:PurchaseItemView ImageSource="spa"
                                           Text="{Binding IndexInsurance}"
                                           Grid.Column="1"/>
                    <view:PurchaseItemView ImageSource="trending"
                                           Text="{Binding EstimateRates}"
                                           Grid.Row="1"/>
                    <view:PurchaseItemView ImageSource="compost"
                                           Text="{Binding Cover}"
                                           Grid.Row="1"
                                           Grid.Column="1"/>
                    <view:PurchaseItemView ImageSource="description"
                                           Text="{Binding SubmittingClaim}"
                                           Grid.Row="2"/>
                    <view:PurchaseItemView ImageSource="quora"
                                           Text="{Binding FAQ}"
                                           Grid.Row="2"
                                           Grid.Column="1"/>
                </Grid>
            </ScrollView>
            <Button Text="{Binding ButtonBuyText}"
                    TextColor="Black"
                    CornerRadius="30"
                    BackgroundColor="#D3BCA2"
                    BorderWidth="1"
                    BorderColor="White"
                    Margin="0,0,0,20"
                    Command="{Binding ClickBuyCommand}"
                    Grid.Row="1"
                    Grid.Column="1"/>
        </Grid>
    </Grid>
    
</ContentPage>